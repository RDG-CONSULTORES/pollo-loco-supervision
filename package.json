{
  "name": "pollo-loco-supervision",
  "version": "1.0.0",
  "description": "El Pollo Loco CAS - Sistema de Supervisión Operativa con Telegram Bot y Mini Web App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npm run server\" \"npm run bot\"",
    "server": "cd backend && npm run dev",
    "bot": "node telegram-bot/bot.js",
    "build": "echo 'Using static HTML files, no build needed'",
    "install-deps": "npm install && cd backend && npm install && cd telegram-bot && npm install",
    "deploy": "npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "telegram-bot",
    "dashboard",
    "supervision",
    "el-pollo-loco",
    "mini-web-app",
    "neon-database"
  ],
  "author": "Roberto Davila",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "concurrently": "^8.2.2",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  }
}
